// 3. Convertir a minúsculas
cadena = prompt("Ingresa una palabra");
const convertirMinusculas = cadena => cadena.toLowerCase();
console.log(convertirMinusculas(cadena));

// 3. Convertir a minúsculas
function convertirMinusculas(cadena) {
    return cadena.toLowerCase();
}
cadena = prompt("Ingresa una palabra");
console.log(convertirMinusculas(cadena));

